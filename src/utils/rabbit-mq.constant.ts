// TODO: fill this
export enum MQ_ROUTING_KEY_CONSTANTS {
  MATCHING = 'match',
}

// TODO: think about versioning
export enum MQ_QUEUE_CONSTANTS {
  MATCHING_QUEUE = '[v1]match.job',
}

export enum MQ_EXCHANGE_CONSTANTS {
  MATCHING = '[v1]Matching',
}
